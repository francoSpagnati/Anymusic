//pagina di Fallback o Offline Page
import React from 'react';

const OfflinePage = () => {
  return (
    <div style={{ textAlign: 'center', padding: '50px' }}>
      <h1>Sei offline o la pagina non è disponibile</h1>
      <p>Controlla la tua connessione e riprova più tardi.</p>
    </div>
  );
};

export default OfflinePage;
